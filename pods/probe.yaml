apiVersion: v1
kind: pod
metadata:
  name: probe
spec:
  containers:
  - images: nginx
    name: nginx
    livelinessprobe:
      httpGet:
        path: /
        port: 80
      InitialDelaySeconds: 3
      PeriodSeconds: 5